rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Warning: This allows public access for development purposes.
      allow read, write: if true;
    }
  }
}
